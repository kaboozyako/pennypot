{
  "name": "@pennypot/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "PennyPot Solidity contracts (Foundry).",
  "scripts": {
    "build": "echo skip",
    "test": "forge test",
    "fmt": "forge fmt",
    "snapshot": "forge snapshot",
    "deploy": "forge script script/Deploy.s.sol:Deploy --rpc-url $RPC_URL --broadcast --verify"
  }
}
